// The canonical account identity lives with the rest of the site profile.
import site from '../content/site.json' with { type: 'json' }

export const GITHUB_USER = site.social.github.user
export const GITHUB_USER_AGENT = site.social.github.user
